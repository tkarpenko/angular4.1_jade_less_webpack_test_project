import { Component }  from '@angular/core';
import { Router, ActivatedRoute }        from '@angular/router';

@Component({
  templateUrl: './admin.component.jade',
  styles : [require('./admin.component.less')]
})

export class AdminComponent {
  constructor() {}
}
